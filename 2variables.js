var a = 1; //Declares a variable globally or locally to a function, with function scope.
const b = 2; //Declares a block-scoped constant variable, cannot be reassigned after declaration.
let c = 3; //Declares a block-scoped variable, allowing reassignment.

console.log(a , b , c);